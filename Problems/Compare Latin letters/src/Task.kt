import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)
    val one = scanner.next().first()
    val two = scanner.next().first()
    println(one.toLowerCase() == two.toLowerCase())
}